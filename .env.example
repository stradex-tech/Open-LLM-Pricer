#
# Copy to .env and fill in real values.
# Never commit .env to GitHub.
#

# Required: used to sign login sessions (>= 16 chars).
# Generate one:
#   openssl rand -hex 32
SESSION_SECRET=

# Optional: bind host interface.
# Default is 0.0.0.0 so other machines can access it; firewall appropriately.
LISTEN_HOST=0.0.0.0

# Optional: if you run behind a reverse proxy (nginx/caddy/traefik), set:
# TRUST_PROXY=true
# COOKIE_SECURE=true

